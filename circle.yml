---
dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install hmmer ncbi-blast+
    - cd ~/ && curl -fsSL get.nextflow.io | bash
    - export PATH=$PATH:$PWD
  cache_directories:
    - "vendor/python"
  override:
    - controls/install

test:
  override:
    - controls/test
