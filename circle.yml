---
dependencies:
  pre:
    - sudo apt-get install hmmer2 ncbi-blast+
    - cd ~/ && curl -fsSL get.nextflow.io | bash
    - export PATH=$PATH:$PWD
  cache_directories:
    - "vendor/python"
  override:
    - controls/install

test:
  override:
    - controls/test
