---
dependencies:
  pre:
    - sudo apt-get install hmmer2 ncbi-blast+
    - curl -fsSL get.nextflow.io | bash
  cache_directories:
    - "vendor/python"
  override:
    - controls/install

test:
  override:
    - controls/test
